# Python
__pycache__/
*.pyc
.venv/
venv/

# Secrets & local config
.env
*.pem
*_accessKeys.csv

# WebUI runtime
code/webui/events.jsonl
code/webui/listings.jsonl

# Data & models (large; not committed — reproducible via prepare_data.py /
# label_condition.py datasets). condition_labels.json IS committed (the labels).
data/
code/image_analyser/data_messy/
code/image_analyser/data_varied/
*.pth

# OS
.DS_Store

# demo/ (recorded video + script) — large, ships in the submission ZIP, not git
demo/

# Streamlit configuration for the Property Triage WebUI.

[client]
# Hide the "Deploy" button and trim the developer menu for a clean app.
toolbarMode = "minimal"

[browser]
gatherUsageStats = false

[theme]
base = "light"
primaryColor = "#2e7d32"

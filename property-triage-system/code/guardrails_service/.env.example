# Guardrails service configuration. Copy to `.env` and adjust if needed.

AWS_PROFILE=course
AWS_REGION=us-east-1

# Bedrock Guardrail (created by scripts/04_create_guardrail.py on 2026-06-10)
GUARDRAIL_ID=huksxm9z68f6
GUARDRAIL_VERSION=DRAFT

# Gemini (key pulled from AWS Secrets Manager)
GEMINI_SECRET_NAME=property-triage/gemini-api-key
GEMINI_MODEL=gemini-2.5-flash
